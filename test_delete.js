import { initializeApp } from "firebase/app";
import { getFirestore, doc, deleteDoc } from "firebase/firestore";
// Need full firebase setup to run node script, so I'll just change the button text and check if there's an error.
